// const score = 400;
// // console.log(score);

// const balance = new Number(300); // explicitly creates a Number object with the value   300. This means that balance is not a primitive number but an instance of the Number object
// console.log(balance);

// console.log(balance.toString()); //it will convert number in to string type 

// console.log(balance.toFixed(2)); //it will set only given value in argument after the point

// const num = 123.456789;
// console.log(num.toPrecision(5)); // "123.46" (Rounded to 5 significant digits)

// const hundred = 10000000;
// console.log(hundred.toLocaleString()); //format a number, date, or time value into a string representation that is tailored to the user's locale


//-----------------------maths--------------------------

console.log(Math.abs(-5)); //it will change minus value into plus

console.log(Math.round(4.6)); //it will give round of value of given value in brackets

console.log(Math.ceil(4.3)); //it will give greater round of value, in this case (5)

console.log(Math.floor(4.3)); //it will give lower round of value, in this case (4)

console.log(Math.sqrt(4));  // it will squareroot of given value, for 4 it will give 2

console.log(Math.pow(2, 4)); // give 2*2*2*2/