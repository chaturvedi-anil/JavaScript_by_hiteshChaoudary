// All important methond of strings

// String.substring(0, 4) 4 is not includede

// String.slice(0,5) it also take negative values

// String.trim() it will remove all the whitespace from starting and end

// String.replace('target-value', 'value for replace') it takes 2 argument 

// String.includes('valueToFind') it will return true if give value is present in string and 
// false if given value is not present in the string 

// String.split('','') it will take two arguments
// split(separator)
// split(separator, limit)


// i have to read all important method of string
